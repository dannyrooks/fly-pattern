import React from 'react' 

const Material = props => {
    return (
        <div>
            <h4>{props.name}</h4>
            <p>{props.description}</p>
        </div>
    )
}

export default Material
